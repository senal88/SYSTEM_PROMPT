
# ============================================================================
# 🔐 1Password Connect – macOS Development (Example)
# Arquivo: env/macos.env.example
# Propósito: Exemplo de configurações específicas do ambiente macOS
# ============================================================================

# ⚙️ Carrega configuração compartilhada
source "$(dirname "${BASH_SOURCE[0]}")/shared.env.example"

# 1Password Connect Configuration
export OP_VAULT=""
export OP_CONNECT_HOST_MACOS=""
export OP_CONNECT_HOST=""

# Environment
export OP_ENVIRONMENT=""
export OP_REGION=""

# Logging
export OP_LOG_LEVEL=""
export OP_LOG_FILE=""

# Security (macOS defaults)
export OP_SESSION_TIMEOUT=""
export OP_VAULT_TIMEOUT=""

# Se o token específico já estiver carregado no shell, replica para a variável genérica
if [[ -n "${OP_CONNECT_TOKEN_MACOS:-}" ]]; then
  export OP_CONNECT_TOKEN=""
fi
