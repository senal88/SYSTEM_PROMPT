
# ============================================================================
# 🔐 1Password Connect – VPS Production (Example)
# Arquivo: env/vps.env.example
# Propósito: Exemplo de configurações específicas do ambiente VPS
# ============================================================================

# ⚙️ Carrega configuração compartilhada
source "$(dirname "${BASH_SOURCE[0]}")/shared.env.example"

# 1Password Connect Configuration
export OP_VAULT=""
export OP_CONNECT_HOST_VPS=""
export OP_CONNECT_HOST=""

# Environment
export OP_ENVIRONMENT=""
export OP_REGION=""

# Logging
export OP_LOG_LEVEL=""
export OP_LOG_FILE=""

# Security (VPS defaults)
export OP_SESSION_TIMEOUT=""
export OP_VAULT_TIMEOUT=""

if [[ -n "${OP_CONNECT_TOKEN_VPS:-}" ]]; then
  export OP_CONNECT_TOKEN=""
fi
