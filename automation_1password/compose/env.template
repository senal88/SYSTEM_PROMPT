# env.template - Stack Plataforma Completa
# Injetar: op inject -i env.template -o .env
# Last Updated: 2025-10-31
# Version: 2.1.0

# === Projeto ===
PROJECT_SLUG=platform
PRIMARY_DOMAIN={{PRIMARY_DOMAIN}}
ENVIRONMENT=production

# === Traefik ===
TRAEFIK_EMAIL={{TRAEFIK_EMAIL}}

# === PostgreSQL + pgvector ===
POSTGRES_USER=postgres
POSTGRES_PASSWORD={{POSTGRES_PASSWORD}}
POSTGRES_DB=platform_db

# === MongoDB ===
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD={{MONGO_ROOT_PASSWORD}}

# === Redis ===
REDIS_PASSWORD={{REDIS_PASSWORD}}

# === Appsmith ===
APPSMITH_SUPER_ADMIN_EMAIL=admin@platform.local
APPSMITH_SUPER_ADMIN_PASSWORD={{APPSMITH_ADMIN_PASSWORD}}
APPSMITH_ENCRYPTION_PASSWORD={{APPSMITH_ENCRYPTION_PASSWORD}}
APPSMITH_ENCRYPTION_SALT={{APPSMITH_ENCRYPTION_SALT}}

# === n8n ===
N8N_ENCRYPTION_KEY={{N8N_ENCRYPTION_KEY}}
N8N_USER_MANAGEMENT_JWT_SECRET={{N8N_JWT_SECRET}}

# === AI / LLM Services (JÁ EXISTEM NO 1P_MACOS) ===
HUGGINGFACE_TOKEN=op://1p_macos/HuggingFace-Token/credential
PERPLEXITY_API_KEY=op://1p_macos/Perplexity-API/credential
GEMINI_API_KEY=op://1p_macos/Gemini-API/credential
CURSOR_API_KEY=op://1p_macos/Cursor-API/credential

# === Cloudflare DNS (JÁ EXISTE NO 1P_VPS) ===
CLOUDFLARE_API_KEY=op://1p_vps/Cloudflare/API_KEY
CLOUDFLARE_API_TOKEN=op://1p_vps/Cloudflare/API_TOKEN
CLOUDFLARE_ZONE_ID=op://1p_vps/Cloudflare/ZONE_ID
CLOUDFLARE_ACCOUNT_ID=op://1p_vps/Cloudflare/ACCOUNT_ID
CLOUDFLARE_EMAIL=op://1p_vps/Cloudflare/EMAIL

# === SMTP (JÁ EXISTE NO 1P_MACOS) ===
SMTP_HOST=op://1p_macos/SMTP/HOST
SMTP_PORT=op://1p_macos/SMTP/PORT
SMTP_USER=op://1p_macos/SMTP/USER
SMTP_PASSWORD=op://1p_macos/SMTP/PASSWORD

# === HuggingFace Pro (1TB) ===
HF_HOME={{HF_HOME}}
HF_DATASETS_CACHE={{HF_DATASETS_CACHE}}
HF_HUB_CACHE={{HF_HUB_CACHE}}
