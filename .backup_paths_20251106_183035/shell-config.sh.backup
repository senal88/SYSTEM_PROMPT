#!/bin/bash
# Configuração Shell Comum
# Carregado por .bashrc ou .zshrc

# Aliases úteis
alias ll='ls -lah'
alias la='ls -la'
alias ..='cd ..'
alias ...='cd ../..'

# 1Password aliases
alias op-check='op-config-check'
alias op-signin='op-signin-auto'

# Git aliases
alias gst='git status'
alias gco='git checkout'
alias gcm='git commit -m'
alias gp='git push'
alias gl='git pull'

# Docker aliases
alias dc='docker-compose'
alias dcu='docker-compose up'
alias dcd='docker-compose down'
alias dcl='docker-compose logs -f'

# Funções úteis
mkcd() {
    mkdir -p "$1" && cd "$1"
}

# Cores para output
export RED='\033[0;31m'
export GREEN='\033[0;32m'
export YELLOW='\033[1;33m'
export BLUE='\033[0;34m'
export NC='\033[0m'

