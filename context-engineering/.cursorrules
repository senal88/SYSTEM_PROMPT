# Cursor Rules - Contexto Global

## 📋 Base de Contexto

Este workspace usa `~/Dotfiles` como padrão global de governança. Sempre consulte:
- `~/Dotfiles/context/global/CONTEXTO_GLOBAL_COMPLETO.md` - Contexto completo
- `~/Dotfiles/context/cursor/CONTEXTO_CURSOR.md` - Contexto específico Cursor

## 🎯 Configurações Padronizadas

- **Base Path**: `~/Dotfiles`
- **Projeto GCP**: `gcp-ai-setup-24410` (501288307921)
- **Região**: `us-central1`
- **Credenciais**: 1Password → `~/Dotfiles/credentials/` (local, não versionado)

## 🔐 Credenciais

- **Fonte**: 1Password (vault: 1p_macos ou Personal)
- **Sincronização**: `~/Dotfiles/scripts/sync/sync-1password-to-dotfiles.sh`
- **Local**: `~/Dotfiles/credentials/` (NUNCA commitar)

## 🚀 Comandos Importantes

```bash
# Setup completo
cd ~/Dotfiles && ./scripts/setup/master.sh

# Sincronizar credenciais
./scripts/sync/sync-1password-to-dotfiles.sh

# Atualizar contexto
./scripts/context/update-global-context.sh
```

## 📁 Estrutura

- `~/Dotfiles/configs/` - Configurações
- `~/Dotfiles/scripts/` - Scripts
- `~/Dotfiles/credentials/` - Credenciais (local)
- `~/Dotfiles/context/` - Contexto para IAs

## ⚠️ Regras

1. **SEMPRE** usar `~/Dotfiles` como base
2. **NUNCA** commitar credenciais
3. **SEMPRE** sincronizar do 1Password antes de usar
4. **SEMPRE** atualizar contexto após mudanças significativas

---

**Última atualização**: 2025-01-17
